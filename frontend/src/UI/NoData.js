import React from 'react';
import styled from 'styled-components';

const StyledDiv = styled.div`
  text-align: center;
  font-size: 24px;
  margin: 10rem 0;
`;
const NoData = () => {
  return <StyledDiv>No data available 📭</StyledDiv>;
};

export default NoData;
